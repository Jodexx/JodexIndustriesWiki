---
id: install
title: Встановлення
sidebar_custom_props:
  icon: FaCogs
---

import FetchVersion from '@site/src/components/FetchVersion';
import CodeBlock from '@theme/CodeBlock';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

[![MavenRepo](https://img.shields.io/badge/Maven-Repository-blue?logo=apachemaven&style=for-the-badge)](https://repo.jodex.xyz/#/releases/com/jodexindustries/jguiwrapper)
[![JavaDocs](https://img.shields.io/badge/JavaDocs-Latest-green?logo=openjdk&style=for-the-badge)](https://repo.jodex.xyz/javadoc/releases/com/jodexindustries/jguiwrapper/api/latest)

## Репозиторій
<Tabs
  defaultValue="maven"
  groupId="build"
  values={[
    {label: 'Maven', value: 'maven'},
    {label: 'Gradle (Groovy DSL)', value: 'gradle-groovy'},
    {label: 'Gradle (Kotlin DSL)', value: 'gradle-kts'}
  ]}>
  <TabItem value="maven">
    <CodeBlock language='xml'>
{
`<repository>
	    <id>jitpack.io</id>
	    <url>https://jitpack.io</url>
</repository>`
}
    </CodeBlock>
  </TabItem>

  <TabItem value="gradle-groovy">
<CodeBlock language="groovy">
{
`maven {
    name "jitpack.io"
    url "https://jitpack.io"
}
`
}
</CodeBlock>
  </TabItem>

  <TabItem value="gradle-kts">
<CodeBlock language="groovy">
{
`
maven {
    name "jitpack.io"
    url "https://jitpack.io"
}
`
          }
    </CodeBlock>
  </TabItem>

</Tabs>


## Встановлення та використання

Фрагменти доступні для Maven, Gradle Groovy та Gradle Kotlin DSL.

Залежно від структури вашого проекту, існує **два способи** використання бібліотеки:

### 1. Використання як самостійний плагін (модуль `api`)

* JGuiWrapper встановлюється як окремий плагін на сервері.
* Ваш плагін просто підключається до його API **без затінення**.

<Tabs
  defaultValue="maven"
  groupId="build"
  values={[
    {label: 'Maven', value: 'maven'},
    {label: 'Gradle (Groovy DSL)', value: 'gradle-groovy'},
    {label: 'Gradle (Kotlin DSL)', value: 'gradle-kts'}
  ]}>
  <TabItem value="maven">
    <FetchVersion type="maven" artifactId="api" projectId="JGuiWrapper" />
  </TabItem>

  <TabItem value="gradle-groovy">
    <FetchVersion type="gradle-groovy" artifactId="api" projectId="JGuiWrapper" />
  </TabItem>

  <TabItem value="gradle-kts">
    <FetchVersion type="gradle-kts" artifactId="api" projectId="JGuiWrapper" />
  </TabItem>
</Tabs>

### 2. Вбудовування у ваш проект (модуль `common`)

* Ви безпосередньо включаєте бібліотеку і **затінюєте** її в кінцевий jar-файл за допомогою `shade` (або `shadowJar`).
* Не потрібно встановлювати JGuiWrapper як окремий плагін.

<Tabs
  defaultValue="maven"
  groupId="build"
  values={[
    {label: 'Maven', value: 'maven'},
    {label: 'Gradle (Groovy DSL)', value: 'gradle-groovy'},
    {label: 'Gradle (Kotlin DSL)', value: 'gradle-kts'}
  ]}>
  <TabItem value="maven">
    <FetchVersion type="maven" artifactId="common" projectId="JGuiWrapper" implementation />

    Якщо ви хочете включити логіку nms для управління заголовками
    <FetchVersion type="maven" artifactId="nms" projectId="JGuiWrapper" implementation />
  </TabItem>

  <TabItem value="gradle-groovy">
    <FetchVersion type="gradle-groovy" artifactId="common" projectId="JGuiWrapper" implementation />
        Якщо ви хочете включити логіку nms для управління заголовками
    <FetchVersion type="gradle-groovy" artifactId="nms" projectId="JGuiWrapper" implementation />
  </TabItem>

  <TabItem value="gradle-kts">
    <FetchVersion type="gradle-kts" artifactId="common" projectId="JGuiWrapper" implementation />
        Якщо ви хочете включити логіку nms для управління заголовками
    <FetchVersion type="gradle-kts" artifactId="nms" projectId="JGuiWrapper" implementation />
  </TabItem>
</Tabs>

#### Загальна ініціалізація

:::warning
Використання бібліотеки без окремого плагіна JGuiWrapper вимагає ініціалізації слухачів у вашому основному класі плагіна.
:::

```java
@Override
public void onEnable() {
    JGuiInitializer.init(this); // реєструє всіх слухачів
}
```

Після виклику `init` ви можете створювати та відкривати графічні інтерфейси для будь-якого гравця

---

## API

Загальнодоступні api методи можна знайти в класі [GuiApi](https://github.com/Jodexx/JGuiWrapper/tree/master/api/src/main/java/com/jodexindustries/jguiwrapper/api/GuiApi.java),
що є батьківським класом `JGuiInitializer`

```java
GuiApi api = GuiApi.get();
```

:::warning
Варто зазначити, що [GuiApi#get()](https://github.com/Jodexx/JGuiWrapper/tree/master/api/src/main/java/com/jodexindustries/jguiwrapper/api/GuiApi.java#L41)
може викидати помилку ініціалізації, так як не було здійснено виклик `JGuiInitializer#init(Plugin)`, що означає, API не було ініціалізовано
:::

### Безпечний доступ до API

Інколи трапляється таке, що ініціалізація може викликатись пізніше певної логіки зі взаємодією з 
[GuiApi](https://github.com/Jodexx/JGuiWrapper/tree/master/api/src/main/java/com/jodexindustries/jguiwrapper/api/GuiApi.java), 
тому варто використати [GuiApi#getOptional()](https://github.com/Jodexx/JGuiWrapper/tree/master/api/src/main/java/com/jodexindustries/jguiwrapper/api/GuiApi.java#getOptional())

```java
GuiApi.getOptional().ifPresent(api -> {
    Plugin plugin = api.getPlugin();
});
```